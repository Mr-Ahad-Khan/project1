{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "Server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env Server.js",
    "check": "node --check Server.js && node --check Config/db.js && node --check Routes/userRoutes.js && node --check Controllers/userController.js && node --check Models/userModels.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mysql2": "^3.22.5",
    "sequelize": "^6.37.8"
  }
}
